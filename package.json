{
  "name": "@opencx/widget-monorepo-root",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "clean": "turbo clean && rm -rf node_modules .turbo",
    "clean:dist": "turbo clean:dist && rm -rf .turbo",
    "gen:sdk": "pnpm -F @opencx/widget-core gen:sdk",
    "build": "turbo build",
    "type-check": "turbo type-check",
    "test": "turbo test",
    "dev:prepare": "dotenv -- turbo dev:prepare --env-mode",
    "dev": "dotenv -- turbo dev --env-mode",
    "cs": "changeset",
    "csv": "changeset version",
    "csp": "changeset publish",
    "prepublishOnly": "pnpm clean:dist && pnpm build && pnpm type-check && pnpm test"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.9",
    "@types/node": "^20.14.8",
    "@vitejs/plugin-react-swc": "^4.0.0",
    "dotenv-cli": "^10.0.0",
    "jsdom": "^25.0.1",
    "prettier": "^3.5.3",
    "turbo": "^2.5.5",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vite-plugin-dts": "4.0.3",
    "vite-plugin-externalize-deps": "^0.9.0",
    "vite-tsconfig-paths": "^5.0.1",
    "vitest": "^3.0.2"
  }
}
